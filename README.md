# Django-Ecommerce-Backend

Backend for my E-Commerce project, built with **Django** and **Django REST Framework**.

I am building the frontend separately using **React** in another repository. I am also learning **Microservice Architecture**, so I am keeping the frontend and backend as separate projects.

## Tech Stack

* Python
* Django
* Django REST Framework
* PostgreSQL
* CORS

## Current Progress

* Django backend setup
* PostgreSQL database configured
* CORS configured
* Backend project structure initialized

🚧 **Project is currently under development.**
